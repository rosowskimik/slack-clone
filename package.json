{
  "private": true,
  "workspaces": [
    "services/*"
  ],
  "name": "slack-clone",
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "gen:client": "lerna run gen --scope=@slack-clone/client"
  }
}
